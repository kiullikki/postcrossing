export interface stateFormReg {
  login: string;
  email: string;
  password: string;
  passwordConfirm: string;
}
export interface stateFormRegError {
  errorLogin: string;
  errorEmail: string;
  errorPassword: string;
  errorPasswordConfirm: string;
}
